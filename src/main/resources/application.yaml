spring:
  application:
    name: estadisticas

  datasource:
    url: jdbc:postgresql://localhost:5433/estadisticas
    username: postgres
    password: 92188162
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true    
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true